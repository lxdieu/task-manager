import colors from './colors';

const lightTextColors = {
  textDefault:colors.dn30,
  textPrimary: colors.dn30,
  textSecondary:  colors.mn50,
  textWarning:  colors.sy70,
  textSuccess:  colors.sg70,
  textError:  colors.sr70,
};

const darkTextColors = {
  textDefault:colors.mn50,
  textPrimary: colors.mn50,
  textSecondary:  colors.dn30,
  textWarning:  colors.sy70,
  textSuccess:  colors.sg70,
  textError:  colors.sr70,
};

export { lightTextColors, darkTextColors };
